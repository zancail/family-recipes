import * as React from "react"

// markup
const Header = () => {
  return (
    <header class="bg-primary text-white py-2 mb-4">
      <div class="container">Family Recipes</div>
    </header>
  )
}

export default Header
